<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserOrderDAO">
	<select id="cartList" resultType="BasketVO" parameterType="BasketVO">
		SELECT * FROM BASKET 
		WHERE U_ID = #{u_id}
	</select>
	<insert id="insertCartItem" parameterType="BasketVO">
	
	</insert>
	
	<update id="updateCartItem" parameterType="BasketVO">
	</update>

	<delete id="deleteCartUser" parameterType="BasketVO">
	</delete>
</mapper>
 