<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TermsDAO">
	<resultMap type="TermsVO" id="termsVO">
		<id column="T_CODE" property="t_code" jdbcType="VARCHAR"/>
		<result column="T_TYPE" property="t_type" jdbcType="VARCHAR" />
		<result column="T_TITLE" property="t_title" jdbcType="VARCHAR" />
		<result column="T_CONTENT" property="t_content" jdbcType="VARCHAR"/>
	</resultMap>
	
	<resultMap type="TermsVO" id="terms_title_VO">
		<result column="T_TITLE" property="t_title" jdbcType="VARCHAR" />
	</resultMap>
	
	<select id="termsList" resultType="TermsVO" resultMap="termsVO">
		SELECT * FROM TERMS WHERE T_TYPE ='term'
		ORDER BY T_CODE
	</select>
	
	<select id="infoList" resultType="TermsVO" resultMap="termsVO">
		SELECT * FROM TERMS WHERE T_TYPE ='privacy'
		ORDER BY T_CODE
	</select>
	
	<select id="termsOne" resultType="TermsVO" resultMap="termsVO">
		SELECT * FROM TERMS WHERE T_CODE = #{t_code}
	</select>
	
	<update id="updateTerms">
        UPDATE TERMS
        SET 
        t_type=#{t_type},
        t_title = #{t_title},
        t_content = #{t_content},
        t_enfdate = #{t_enfdate},
        t_regdate = #{t_regdate}
        WHERE
        t_code = #{t_code}
    </update>
    
    <select id="tCodeMax" resultType="int" >
		SELECT MAX(CAST(SPLIT_PART(T_CODE::VARCHAR,'_',2) as INTEGER)) from TERMS 
		WHERE T_TYPE='term';
		</select>
		
		<select id="infoOne" resultType="TermsVO" resultMap="termsVO">
		SELECT * FROM TERMS WHERE T_CODE = #{t_code}
		</select>
    
	<select id="tPolicyCodeMax" resultType="int" >
		SELECT MAX(CAST(SPLIT_PART(T_CODE::VARCHAR,'_',2) as INTEGER)) from TERMS 
		WHERE T_TYPE='privacy';
	</select>
	
	<insert id="insertPolicy">
	INSERT INTO
    	TERMS(T_TYPE, T_CODE, T_TITLE, T_CONTENT, T_REGDATE ) 
    	VALUES (#{t_type},#{t_code} ,#{t_title}, #{t_content}, #{t_regdate})
	
	</insert>
	
	<delete id="deletePol">
		DELETE FROM TERMS WHERE T_CODE = #{T_CODE}
	</delete>
	
	
	
	
	
</mapper>
